version: '3'
services:
  rudydb:
    build:
      context: ../.
      dockerfile: build/rudydb/Dockerfile
    command: rudydb
    environment:
      - ADDR=0.0.0.0:80
      - GIN_MODE=debug
    ports:
      - 3000:80
